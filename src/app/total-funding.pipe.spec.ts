import { TotalFundingPipe } from './total-funding.pipe';

describe('TotalFundingPipe', () => {
  it('create an instance', () => {
    const pipe = new TotalFundingPipe();
    expect(pipe).toBeTruthy();
  });
});
